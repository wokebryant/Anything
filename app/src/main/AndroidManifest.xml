<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wokebryant.anythingdemo">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:name=".Application.BaseApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.ViewFlipperActivity" />
        <activity android:name=".PersonSetting.Activity.PersonalSettingActivity"/>
        <activity android:name=".PersonSetting.Activity.ClipImageActivity"/>
        <activity android:name=".PersonSetting.Activity.ChatSettingActivity"/>
        <activity android:name=".DynamicsItem.activity.PersonalDynamicsActivity"/>
        <service android:name=".PersonSetting.Audio.SoundRecorderService"/>

    </application>

</manifest>